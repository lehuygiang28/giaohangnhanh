export type ApiPath = {
    /**
     * Đường dẫn API, sử dụng khi api từ Ghn thay đổi
     * 
     * @en
     * API path, used when api from Ghn is changed
     * 
     * @example 'shiip/public-api/v2/shipping-order/cancel'
     * 
     */
    apiPath?: string;
}